# =============================================================================
# job-research Dashboard (Vite) — переменные окружения (скопировать в .env)
# =============================================================================

VITE_APP_HOST=dashboard.job-research.loc
VITE_APP_PORT=3001

# Базовый URL REST API со суффиксом /api (axios baseURL)
VITE_API_URL=http://api.job-research.loc:3000/api
