---
description: NestJS-соглашения, Drizzle-паттерны, архитектура API. Читать при работе с apps/api/.
globs: apps/api/**/*
alwaysApply: false
---

# Backend (NestJS 11) — правила и паттерны

## Структура модуля NestJS

Каждый модуль — папка `src/<module>/`:

```
<module>/
  <module>.module.ts       — @Module() декоратор
  <module>.controller.ts   — HTTP-эндпоинты (опционально)
  <module>.service.ts      — бизнес-логика
  dto/                     — входящие данные (созданы из zod через nestjs-zod или вручную)
  guards/                  — @UseGuards()
  strategies/              — Passport-стратегии
```

## Инжектирование Drizzle

Drizzle не инжектируется через NestJS DI по классу — используется Symbol:

```typescript
import { Inject, Injectable } from '@nestjs/common'
import { DRIZZLE, type DrizzleDb } from '../database/database.module.js'

@Injectable()
export class SomeService {
  constructor(@Inject(DRIZZLE) private db: DrizzleDb) {}
}
```

## Схемы БД (Drizzle)

Все таблицы в `src/database/schema/*.ts`, экспортируются через `src/database/schema/index.ts`.

Паттерн таблицы:

```typescript
import { pgTable, serial, text, timestamp } from 'drizzle-orm/pg-core'

export const things = pgTable('things', {
  id: serial('id').primaryKey(),
  name: text('name').notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
})

export type Thing = typeof things.$inferSelect
export type NewThing = typeof things.$inferInsert
```

После изменения схемы:

```bash
pnpm --filter api drizzle-kit push   # применить к dev БД
pnpm --filter api drizzle-kit generate  # сгенерировать migration SQL
```

## Конфигурация (ConfigService)

Все env-переменные типизированы через `AppConfig` (`src/config/config.schema.ts`):

```typescript
import { ConfigService } from '@nestjs/config';
import type { AppConfig } from '../config/config.schema.js';

constructor(private configService: ConfigService<AppConfig, true>) {}

// Использование:
const secret = this.configService.getOrThrow('JWT_ACCESS_SECRET');
```

**URL внешних и внутренних сервисов** (`DASHBOARD_URL`, `LANDING_URL`, `HH_API_BASE`, …) — только из `ConfigService` / `config.schema.ts`.

**LLM:** провайдер и модель — из тела LLM-запросов + `settings` (`LlmRuntimeContext`); ключи только в env. **`GET /llm/status`** — только `@Roles('admin')`.

**Telegram:** интеграция в `TelegramModule/TelegramService`. Глобальный `AllExceptionsFilter` отправляет `5xx` в групповой чат (`TELEGRAM_ERRORS_CHAT_ID`). `SearchService` отправляет пользователю краткий дайджест вакансий (название + ссылка) в `users.telegram_chat_id` после завершения прогона. Настройка пользователем — `POST /api/auth/telegram/connect` и `POST /api/auth/telegram/disconnect`.

## Ошибки и корреляция

- Каждый запрос получает **`X-Request-Id`** (или клиентский заголовок сохраняется); то же значение в JSON ошибок как **`requestId`**.
- Глобальный **`AllExceptionsFilter`** дописывает `requestId` к ответам `HttpException` и к 500-м «необработанным» исключениям, а также шлёт уведомления по `5xx` в Telegram-группу (если заполнены env Telegram).

## Swagger / OpenAPI

- UI: **`GET /docs`** (без глобального префикса `/api`) — только если `NODE_ENV !== 'production'` или `SWAGGER_ENABLED=true`.
- Offline export: **`pnpm api:openapi:export`** → `apps/api/openapi/openapi.json` (OpenAPI 3.1, без запущенного сервера).
- Схемы: zod в `@repo/shared` → `createZodDto` → `@ApiBody` / `@ApiOkResponse`. Документ: `cleanupOpenApiDoc()` в `swagger-document.ts`.

## Авторизация

- `JwtAuthGuard` + `RolesGuard` — глобально через `APP_GUARD`
- JWT payload: `{ sub, email, role }` — role из БД в `JwtStrategy.validate`
- Refresh/access — JSON + localStorage на dashboard (см. `auth.mdc`)
- `bcryptjs` — пароли (cost 12), refresh hash (cost 10)
- Rate limit: `@Throttle` на `/auth/login`, `/auth/register`
- `helmet` в `main.ts`

## Валидация входящих данных

Zod-схемы живут в `packages/shared/src/schemas/`.

В контроллерах: `safeParse` + `BadRequestException(flatten())`:

```typescript
const parsed = ExampleSchema.safeParse(body)
if (!parsed.success) throw new BadRequestException(parsed.error.flatten())
```

## Логирование

Использовать `Logger` из `@nestjs/common` (не из nestjs-pino напрямую):

```typescript
import { Injectable, Logger } from '@nestjs/common'

@Injectable()
export class MyService {
  private readonly logger = new Logger(MyService.name)

  doSomething() {
    this.logger.log('Выполняю действие')
    this.logger.debug('Детали отладки')
    this.logger.error('Ошибка', error.stack)
  }
}
```

## SSE (Server-Sent Events)

Поиск (`SearchModule`): `POST /api/search/run` после `validateBeforeRun` (в т.ч. `assertLlmAvailable` → 503) запускает `executeRun` в фоне; прогресс — `GET /api/search/stream` + `@Sse()`, `ReplaySubject` на `userId`, события JSON в `MessageEvent.data` (типы `SearchStreamEvent` в `@repo/shared`). Клиент с `EventSource` передаёт JWT в query `access_token` (см. `JwtStrategy`).

## Именование файлов

- Файлы: `kebab-case.ts` (например, `jwt-auth.guard.ts`)
- Классы: `PascalCase` (например, `JwtAuthGuard`)
- Переменные/методы: `camelCase`

## Импорты

- Внутри модуля: относительные (`./service.js`, `../database/schema/index.js`)
- `@repo/shared` — через path alias (tsconfig paths)
- Все `.js` расширения в импортах (ESM-совместимость в CJS-билде через tsc)
